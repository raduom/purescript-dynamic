{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp build -I test -- --censor-lib --strict && pulp test"
  },
  "devDependencies": {
    "pulp": "^10.0.0",
    "purescript": "^0.10.5",
    "purescript-psa": "^0.4.0",
    "rimraf": "^2.5.4"
  }
}
